{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "human.gate input",
  "type": "object",
  "required": ["question", "options"],
  "properties": {
    "question": {
      "type": "string",
      "description": "The prompt shown to the user in the PWA."
    },
    "options": {
      "type": "array",
      "description": "The choices the user picks between. Free text labels.",
      "items": { "type": "string" },
      "minItems": 2
    },
    "preview": {
      "description": "Optional context object rendered above the question (e.g. a draft verdict, a diff link, a JSON object). Free-form."
    }
  },
  "additionalProperties": false
}
