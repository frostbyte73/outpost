{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "code.spec output",
  "description": "The real output is delivered via mcp__outpost__submit_spec, not the session's final message. This schema documents that payload's shape.",
  "type": "object",
  "required": ["spec"],
  "properties": {
    "spec": {
      "type": "string",
      "description": "Design spec markdown."
    }
  }
}
